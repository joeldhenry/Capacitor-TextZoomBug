import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'text-zoom-bug',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
